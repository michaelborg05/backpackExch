profiles:
  default:
    display_name: "signal_trading_15m"
    enabled: true
    api_key_env: BACKPACK_API_KEY
    secret_env: SECRET

    strategy_type: trend_following

    take_profit_pct: 1.2
    stop_loss_pct: 0.7
    trailing_stop_pct: 0.8
    arm_trailing_stop_pct: 0.7
    use_trailing_stop: true

    #position sizing
    default_order_size_usdc: 100.0      # Buy $100 per order
    max_position_size_pct: 40.0         # Max 40% of portfolio per symbol
    
    # Risk management
    max_open_positions: 3               # Max concurrent positions per token
    max_portfolio_exposure_pct: 100.0    # Max 100% in positions total

    use_market_regime_filter: true

    # 🎯 SIGNAL GENERATION SETTINGS (NEW)
    enable_signal_generation: true    # Enable automated signal generation
    signal_timeframe: "15"           # Timeframe for entry signals
    signal_cooldown_seconds: 900      # Wait 15min between signals for same symbol
    min_signal_confidence: 70.0       # Only trade signals >= 75% confidence
    min_volume_ratio: 1.1             # Require 10% above average volume

    # Trend filters
    use_trend_filter: true
    trend_timeframe: "60"  # Check 60 (1hr) trend for 15m trades
    trend_indicators:
      - type: "ema_slope"  # NEW: Ensure 60m trend is rising
        params:
          ema: 50
          direction: "not_falling"
          min_slope_pct: 0.01
      - type: "ema_slope"  # NEW: Ensure 60m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.01
      - type: "rsi_threshold"
        params: 
          period: 14
          min_value: 52
          use_momentum: true
          early_threshold: 40
      - type: "rsi_overbought"
        params:
          min_value: 70
          hard_stop: true
      - type: "price_vs_vwap"
        params: {}
    min_indicators_required: 3  # Require 3/5

    use_entry_filter: true
    entry_timeframe: "15"  # Same as signal_timeframe for single-TF strategy
    entry_indicators:
      - type: "price_vs_ema"  # NEW: Price must be at/above EMA20
        params:
          ema: 20
          min_gap_pct: -0.5
          max_gap_pct: 1.5
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.02
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 50
          direction: "not_falling"
          min_slope_pct: 0.01
      - type: "rsi_threshold"
        params:
          period: 14
          min_value: 53
          use_momentum: true
          early_threshold: 40
          hard_stop: true
      - type: "rsi_overbought"
        params:
          min_value: 70
          hard_stop: true
      - type: "price_vs_vwap"
        params: {}
    min_entry_indicators_required: 5

    # ATR filter (volatility)
    use_atr_filter: false
     # Trend-based exit (intelligent)
    use_trend_invalidation_exit: false
    trend_invalidation_indicators: entry #trend or entry
    min_position_age_for_trend_check: 60  # minutes
    # Time-based invalidation and max position backstop (last resort)
    max_position_hours: 12  # Force exit if stuck

  15m_MB:
    display_name: "15m_trend_invalidation"
    enabled: true
    api_key_env: BP_API_KEY_15M
    secret_env: BP_SECRET_15M

    strategy_type: trend_following

    take_profit_pct: 1
    stop_loss_pct: 0.7
    trailing_stop_pct: 0.5
    arm_trailing_stop_pct: 0.4
    use_trailing_stop: true

    #position sizing
    default_order_size_usdc: 100.0      # Buy $100 per order
    max_position_size_pct: 40.0         # Max 40% of portfolio per symbol
    
    # Risk management
    max_open_positions: 2               # Max concurrent positions per token
    max_portfolio_exposure_pct: 100.0    # Max 100% in positions total

    use_market_regime_filter: true

    # 🎯 SIGNAL GENERATION SETTINGS (NEW)
    enable_signal_generation: true    # Enable automated signal generation
    signal_timeframe: "15"           # Timeframe for entry signals
    signal_cooldown_seconds: 900      # Wait 15min between signals for same symbol
    min_signal_confidence: 70.0       # Only trade signals >= 75% confidence
    min_volume_ratio: 1.1             # Require 10% above average volume

    # Trend filters
    use_trend_filter: false
    trend_timeframe: "60"  # Check 60 (1hr) trend for 15m trades
    trend_indicators:
      - type: "ema_slope"  # NEW: Ensure 60m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.02
      - type: "rsi_threshold"
        params: 
          period: 14
          min_value: 52
          use_momentum: true
          early_threshold: 40
      - type: "price_vs_vwap"
        params: {}
    min_indicators_required: 2  # Require 3/4 (stricter)

    use_entry_filter: true
    entry_timeframe: "15"  # Same as signal_timeframe for single-TF strategy
    entry_indicators:
      - type: "price_vs_ema"  # NEW: Price must be at/above EMA20
        params:
          ema: 20
          min_gap_pct: -0.5
          max_gap_pct: 1.5
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.02
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 50
          direction: "not_falling"
          min_slope_pct: 0.01
      - type: "rsi_threshold"
        params:
          period: 14
          min_value: 53
          use_momentum: true
          early_threshold: 40
          hard_stop: true
      - type: "rsi_overbought"
        params:
          min_value: 70
      - type: "price_vs_vwap"
        params: {}
    min_entry_indicators_required: 5
    # ATR filter (volatility)
    use_atr_filter: false
    # Trend-based exit (intelligent)
    use_trend_invalidation_exit: true
    trend_invalidation_indicators: entry #trend or entry
    min_position_age_for_trend_check: 30  # minutes
    # Time-based invalidation and max position backstop (last resort)
    max_position_hours: 12  # Force exit if stuck
 
  profile3:
    display_name: "swing_4h1h_simple"
    enabled: true
    api_key_env: BP_API_KEY_1M
    secret_env: BP_SECRET_1M
    
    strategy_type: trend_following

    take_profit_pct: 5.0        # Target 8% gains on swings
    stop_loss_pct: 4.0          # 4% initial stop (wider for volatility)
    trailing_stop_pct: 3.5      # Trail with 3% buffer
    arm_trailing_stop_pct: 3.0  # Arm TSL after 4% gain
    use_trailing_stop: true

    #position sizing
    default_order_size_usdc: 100.0      # Buy $100 per order
    max_position_size_pct: 50.0         # Max 40% of portfolio per symbol
    
    # Risk management
    max_open_positions: 2               # Max 5 concurrent positions
    max_portfolio_exposure_pct: 100.0    # Max 80% in positions total

    use_market_regime_filter: true

    # ========================================
    # MULTI-TIMEFRAME STRATEGY (4h + 1h)
    # ========================================
    # Signal generation on 1h bars
    enable_signal_generation: true
    signal_timeframe: "60"           # 1h execution timeframe
    signal_cooldown_seconds: 3600    # 1h between signals
    min_signal_confidence: 78.0      # Higher bar for 1h
    min_volume_ratio: 1.3            # 10% above average

    # ========================================
    # 4H TREND AUTHORITY VALIDATION
    # ========================================
    # This validates the HIGHER timeframe (4h) for trend confirmation
    use_trend_filter: true
    trend_timeframe: "240"  # 4h bars
    
    trend_indicators:
      - type: "rsi_reversal_momentum"
        params:
          lookback_candles: 5        # Last 20 hours (5 x 4H)
          oversold_threshold: 30      # Must have been <30
          current_min: 35             # Current must be >35
          min_jump: 5.0               # Must have jumped >5 at some point
          require_sustained: true     # Must be rising for 2+ candles
          hard_stop: true             # BLOCK if fails
      
      # 2. RSI NOT OVERBOUGHT (SAFETY)
      - type: "rsi_overbought"
        params:
          min_value: 65
          hard_stop: true
      
      # 3. VOLUME SPIKE (CAPITULATION CONFIRMATION)
      # Higher threshold - need real spike, not just elevated
      - type: "volume_spike"
        params:
          min_ratio: 2.0              # Raised from 1.5 (need bigger spike)
          max_ratio: 10.0
      
      # 4. EMA20 STABILIZING
      - type: "ema_slope"
        params:
          ema: 20
          direction: "not_falling"
          min_slope_pct: 0.05
    
    # STRICT: Need 3/4 (all except maybe EMA slope)
    min_indicators_required: 3
    
    # ========================================
    # 1H EXECUTION VALIDATION (via entry_indicators)
    # ========================================
    # These check the EXECUTION timeframe (1h) for entry timing
    
    use_entry_filter: true  # Enable execution timeframe validation
    entry_timeframe: "60"   # Same as signal_timeframe (1h)
    
    entry_indicators:
      - type: "rsi_reversal_momentum"
        params:
          lookback_candles: 5        # Last 5 hours
          oversold_threshold: 35      # Less extreme on 1H
          current_min: 40             # Current must be >40
          min_jump: 3.0               # Smaller jump ok on 1H
          require_sustained: true
          hard_stop: true
      
      # 2. 1H RSI NOT TOO HOT
      # Make sure we're not chasing
      - type: "rsi_overbought"
        params:
          min_value: 60              # Block if already >60
          hard_stop: true
      
      # 3. PRICE VS EMA20 (LESS RESTRICTIVE)
      - type: "price_vs_ema"
        params:
          ema: 20
          min_gap_pct: -2.0
          max_gap_pct: 5.0
      
      # 4. EMA20 STABILIZING
      - type: "ema_slope"
        params:
          ema: 20
          direction: "not_falling"
          min_slope_pct: 0.02
      
      # 5. VOLUME CONFIRMATION
      - type: "volume_spike"
        params:
          min_ratio: 1.2              # At least elevated
          max_ratio: 5.0
    
    # Need 4/5 (all except maybe one)
    min_entry_indicators_required: 4
    
    use_atr_filter: false
    use_trend_invalidation_exit: true
    trend_invalidation_indicators: entry
    min_position_age_for_trend_check: 240
    
    max_position_hours: 72

  15m_MB_ATR:
    display_name: "mean_reversion_profile"
    enabled: true
    api_key_env: BP_API_KEY_1M_ATR
    secret_env: BP_SECRET_1M_ATR
    
    strategy_type: mean_reversion

    take_profit_pct: 1
    stop_loss_pct: 1
    trailing_stop_pct: 0.4
    arm_trailing_stop_pct: 0.5
    use_trailing_stop: true

    #position sizing
    default_order_size_usdc: 100.0      # Buy $100 per order
    max_position_size_pct: 30.0         # Max 30% of portfolio per symbol

    # Risk management
    max_open_positions: 2               # Max concurrent positions per token
    max_portfolio_exposure_pct: 100.0    # Max 100% in positions total

    use_market_regime_filter: true

    # 🎯 SIGNAL GENERATION SETTINGS (NEW)
    enable_signal_generation: true    # Enable automated signal generation
    signal_timeframe: "15"           # Timeframe for entry signals
    signal_cooldown_seconds: 900      # Wait 15min between signals for same symbol
    min_signal_confidence: 75.0       # Only trade signals >= 75% confidence
    min_volume_ratio: 1.2             # Require 20% above average volume

    # Trend filters
    use_trend_filter: false

    use_entry_filter: true
    entry_timeframe: "15"  # Same as signal_timeframe for single-TF strategy
    entry_indicators:
      # 1. RSI oversold and turning up
      - type: "rsi_oversold"
        params:
          max_value: 40
          require_rising: true
          min_momentum: 2
          hard_stop: true

      # 2. Price below VWAP (stretched)
      - type: "price_below_vwap"
        params:
          min_gap_pct: -0.7       # At least 1% below
          max_gap_pct: -8.0       # Not more than 8% (too risky)
      
      # 3. Price extended below EMA20
      - type: "price_extended_below_ema"
        params:
          ema: 20
          min_gap_pct: -0.7       # at least below ema
          max_gap_pct: -10.0
      
      # 4. Volume spike (capitulation)
      - type: "volume_spike"
        params:
          min_ratio: 1.1
          max_ratio: 5.0
      
      # 5. Bollinger Band touch (if you implement it)
      # - type: "bollinger_bands"
      #   params:
      #     band: "lower"
      #     mode: "touch"
      #     tolerance_pct: 0.5
      
      # 6. NOT overbought on higher timeframe (sanity check)
      - type: "rsi_overbought"
        params:
          min_value: 65  # Fail if RSI > 70 (don't buy tops)
    
    min_entry_indicators_required: 4  # Need 4/5 or 4/6
    # ATR filter (volatility)
    use_atr_filter: false
    atr_timeframe: "15m"
    atr_threshold: 1.15
    atr_filter_mode: "require_low"
    # Trend-based exit (intelligent)
    use_trend_invalidation_exit: false
    trend_invalidation_indicators: trend #trend or entry
    # Time-based invalidation and max position backstop (last resort)
    max_position_hours: 8  # Force exit if stuck

  15m_no_trend:
    display_name: "signals_no_1hrchecks"
    enabled: true
    api_key_env: BP_API_KEY_1H
    secret_env: BP_SECRET_1H

    strategy_type: trend_following

    take_profit_pct: 0.9
    stop_loss_pct: 0.7
    trailing_stop_pct: 0.4
    arm_trailing_stop_pct: 0.4
    use_trailing_stop: true

    #position sizing
    default_order_size_usdc: 100.0      # Buy $100 per order
    max_position_size_pct: 40.0         # Max 40% of portfolio per symbol
    
    # Risk management
    max_open_positions: 2               # Max concurrent positions per token
    max_portfolio_exposure_pct: 100.0    # Max 100% in positions total

    use_market_regime_filter: true

    # 🎯 SIGNAL GENERATION SETTINGS (NEW)
    enable_signal_generation: true    # Enable automated signal generation
    signal_timeframe: "15"           # Timeframe for entry signals
    signal_cooldown_seconds: 900      # Wait 15min between signals for same symbol
    min_signal_confidence: 70.0       # Only trade signals >= 75% confidence
    min_volume_ratio: 1.1             # Require 10% above average volume

    # Trend filters
    use_trend_filter: false
    trend_timeframe: "60"  # Check 60 (1hr) trend for 15m trades
    trend_indicators:
      - type: "ema_slope"  # NEW: Ensure 60m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.02
      - type: "rsi_threshold"
        params: 
          period: 14
          min_value: 52
          use_momentum: true
          early_threshold: 40
      - type: "price_vs_vwap"
        params: {}
    min_indicators_required: 2  # Require 3/4 (stricter)

    use_entry_filter: true
    entry_timeframe: "15"  # Same as signal_timeframe for single-TF strategy
    entry_indicators:
      - type: "price_vs_ema"  # NEW: Price must be at/above EMA20
        params:
          ema: 20
          min_gap_pct: -0.5
          max_gap_pct: 1.5
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 20
          direction: "rising"
          min_slope_pct: 0.02
      - type: "ema_slope"  # NEW: Ensure 15m trend is rising
        params:
          ema: 50
          direction: "not_falling"
          min_slope_pct: 0.01
      - type: "rsi_threshold"
        params:
          period: 14
          min_value: 53
          use_momentum: true
          early_threshold: 40
          hard_stop: true
      - type: "rsi_overbought"
        params:
          min_value: 70
      - type: "price_vs_vwap"
        params: {}
    min_entry_indicators_required: 5

    # ATR filter (volatility)
    use_atr_filter: false
     # Trend-based exit (intelligent)
    use_trend_invalidation_exit: true
    trend_invalidation_indicators: entry #trend or entry
    min_position_age_for_trend_check: 360  # minutes
    # Time-based invalidation and max position backstop (last resort)
    max_position_hours: 12  # Force exit if stuck
