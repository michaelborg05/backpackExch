TO DO
# validate balances against open positions. If something doesn't match, sell it (No open positions but still have a balance)
# make dynamic stop losses as per chat gpt suggestions
# clean up the trend cache to remove the unused reasons summaries. Add it to the validation and remove the column from trades table
# Add profile param for how many indicators needed for trend invalidation
# possibility - Add price momentum check to TI: If price drops below entry within first 10 minutes, treat as instant TI
#ability to add a deposit/withdrawal in the system to update snapshotdata and not let it trigger a circuit breaker
smarter TP/SL - Different per token. Maybe variable based on market
# add backtest to web UI
#clean up circuit breaker
    - Reporting percent from start but triggering from highest - I.e. Reported CB trigger 1.5% loss even though loss limit was 3%. It was looking at highest price for period instead of cb_trigger amount. Would nee dto 
# Add TP and SL from AI suggestion to override defaults
#reduce db reads
# Get AI to remove the indicators/symbols tab from main page but merge he indicator changes into profiles tab where it has a form to fill out
# web page changes
 - Api explorer. Is it dynamic? 
 - Is positions tab working?
  - Market tab - clean up
  - When switching to propfiles tab can i keep the tab menu?
  - When switching between profiles/index.html is there a smoother way to keep the session?
No need
 # split into separate modules so i can migrate to Azure
 # look at option to extend profit % by 50% if all 3 trend indicators are bullish
 # remove trades table. not required unless i use this for limit orders to track whats open?
# possibly sell as MAX if no other positions available
 # fix SL/TP override. Its changing the profile for all future alerts
# If re-entering within 3 hours of TP, extra requirements:
post_tp_reentry_rules:
  time_window_seconds: 10800
  require_price_below_previous_high: true  # Must enter BELOW previous TP high
  min_volume_ratio_override: 1.5  # Higher volume requirement
 post_tp_cooldown_seconds: 7200

 DONE
# Add arming of trailing stop only after x percent profit
# if we fail to close a position, confirm we close the posiution if it is out of funds
 # fix up closing invalid positions by reducing to whats left in the account.
 # For telegram listener, when sending B , get balance for all profiles or specific profile passed
 # double check the UpdateTickersRequest TickerRequest classes are in the right one. I've moved them to market builder but maybe they shoule be somewhere else
 # In api_server, move the functions to another class.
 # Add validation on buy to make sure i have enough funds and reduce
# Change rounding logic to be based off the minimal unit for each symbol. And functionalise it so code is not fuplicated in in _validate_and_adjust_order. Also handle BUY rounding also
 # change the validate rounding of Sell and buy amounts to be the decimal places as specified in https://api.backpack.exchange/api/v1/markets
# Track profit/loss per trade
  # track buys and sells to check profit/loss
  #For profiles, we also need to save balances and portfolio per profile
 # Add profiles support - To trade from multiple accounts
 # Different API keys per profile
# filter out balances for symbols no longer supported
# Add trailing stop loss once certain percent is hit. I.e. Once 5% profit, set a trailing stop loss at 2%
  # Add take profit/stop loss options
 # If cache isnt up to date, go make live calls
 # introduce something to avoid down days. Daily trend filter
#Minimum viable trend filter (simple + effective)
#Only allow 15m buys if ALL are true:
#1H close > 1H EMA50
#1H RSI > 50
#Last 1H candle is green
#This alone would likely flip your 25% win rate closer to 35–40%
# when stop loss armed, set trailing stop loss price to sl % or entry + 0.05% - Which ever is higher
# ATR implementation
# adjust RSI indicator. Either > 50 or > 40 and increasing. similarly maybe below 50 is no, or below 60 and decreasing is also no
 # loss/gain circuit breakers 
 #  5% gain or loss circuit breaker trading pause. I.e. If profile drops by X% within 24 hours or shorter, pause trading for 12 hours
 # Add dust conversion with the validation loop
# move some settings like circuit breaker percents, hours remaining, etc, to db settings table
# add various data to database so its not reset on reboot
     - Circuit breaker  DONE
     - Trend data - No need . Data is sent regularly
 # also consider adding RSI oversold indicators to not buy on 15m/1hr timeframes if overbought
#Look at possibly getting a new buy indicator. Seems to indicate on trend confirmation insted of reversals so buy is too late
# time space trades on 1m to reduce noise. After a TP or SL maybe pause for 5mins
# convert all logs to self.logger
 see claude search titled "Trading signal generator optimization and re-entry strategy" - starred. Need to implement RSI momentum logic and blocked signal logging if I think its useful
 # Move buy amounts for signal generator into yaml - Maybe combine with percentage
 # change buy logic from signal generator to buy X amount up to max amount . Maybe track max buys in a separate config in db? I.e. SOL/ETH Max 30%, hype, sui max 20%
# when restarting service, reproces trend data. Maybe store past 5 in db
# double check rsi momentum is being counted when deciding on trade - or weight it higher
# change circuit breaker  baseline to highest price so it will only ever lose % from highest portfolio price#fix circuit breaker logic. Changed baseline to always be highest price but now it means 5% profit will never hit as baseline keeps climbing
#fix circuit breaker logic. Changed baseline to always be highest price but now it means 5% profit will never hit as baseline keeps climbing
# change TP to a limit order . Need to track open orders and sell if it hits SL or goes above TP and doesnt trigger
# Mean reversion profile for extreme RSI ranges
#Figure out a way to make reason_summary in db more AI readable for analysis
# Add a call on stop loss to run trend/entry rules and add to db 
#telegram - show open positions with profit/loss on each
#change cooldown logic. Maybe make it params in settings. Takeprofit cooldown 35mins, stop loss 35mins, others 15 or 20
#raise alert if no trend data received in 10mins
#Fix up the telegram summary to be more concise
#figure out how to do long term trading profile 
Improvement: The "Mean Reversion" Check
  Still need to add scoring to signal_generator for mean_reversion strategy
  Need to update the pine script to include new indicator info
# review choppy regime and also find out how to configure profiles to handle choppy and high risk if possible
# add indicator to close above the last 3 candle highs - close > highest(high, 3) for entry indicator default profile#Add sys param table to # include things like system settings, defaults. Etc. Can make a profile Id 0 column and eventually add profile override option
# add generic profile names and adda profile desc which shows on alerts and msgs so i can easily change what the profile is doing
# anothr possible idea - Post-TP Re-Entry Protection - yaml# After TP exit, block re-entry on same symbol for 2 hours - 
# move TP/SL limits to database
#review choppy conditions logic - When EMAs are close together it classes as choppy but doesnt take into acct the slope. I.e. If ema20 was way below and is now climbing, once it gets close it will say choppy when it actually is in recovery
#Review mean reversion profile. Need to be able to catch higher falls. I.e. Based on percent or something other than just a hard rsi
# add an audit log on indicators table so i can trace back to a change in behaviour to a specific edit
# clean batch files of passwords and cycle all API keys before going public
# in position summary in Telegram, have small indicator to show if tsl armed
#Fix lookup of symbols on profile - Adding sumbols doesnt show them in the grid
#Bug - When adding new profile and trying to add symbols, getting 404 error Profile spike_entry not found <b>Endpoint:</b> GET /config/symbols/spike_entry
#build solution to add a new profile cleanly
#For range trading maybe check BB slope or width, etc
#Attack vector - Added user_id as an atribute in CreateProfile (profiles.html) to then link the profile to a user. Could insert different user id here
#Ability to add multiple profiles on same acct. Does it cause issues? How to only display balances 1 per acct if multiple profiles using same acct
#web page
# - Audit log - Filter by profile
